#include "generic_allocator.h"

typedef struct GenericAllocator
{
	Allocators allocators;
} GenericAllocator;

uintptr_t GetAllocator(size_t size)
{

	//if (size < slot) {

	//}
	return 0;
}